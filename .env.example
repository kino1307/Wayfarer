ANTHROPIC_API_KEY=sk-ant-...
PORT=3001

# Optional: override the API base URL (leave empty for local dev proxy)
VITE_API_BASE=

# Optional: pre-fill the API key in the UI (leave empty to require manual entry)
VITE_DEFAULT_API_KEY=
